/**
 * **이 파일의 export 이름은 ASCII 여야 한다.** `app/composables` 는 auto-import 대상인데
 * unimport 가 export 명을 ASCII 식별자까지만 읽어서, `use테마pane` 으로 두면
 * `.nuxt/imports.d.ts` 에 `export { use }` 로 등록되고 호출부가 죽는다(실측).
 * 컴포넌트(`l/테마.vue`)는 Vue 의 컴포넌트 해석이라 한글이어도 멀쩡하다.
 */

/**
 * 테마 손잡이를 우측 도크에 띄운다.
 *
 * **팝오버가 아니라 도크 pane 인 이유: 테마의 미리보기는 카드 몇 장이 아니라 뒤에 깔린 화면 전체다.**
 * 팝오버는 그 화면 **위에** 떠서 정작 봐야 할 것을 손잡이가 덮었고, 클릭 한 번에 닫혀서
 * 스크롤하며 축을 굴려볼 수도 없었다. 도크는 덮지 않고 `--dock-inset` 으로 본문을 **민다**.
 *
 * 본문이 좁아지는 값은 치른다. 다만 그건 **AI도우미를 열 때 이미 내고 있는 값**이라
 * 테마가 새로 만드는 비용이 아니고, 폭이 흔드는 것(반응형 단수·표 칼럼)과 테마 축이 나르는 것
 * (색·진하기·재질·그림자·글꼴)이 겹치지 않는다.
 *
 * ## 2026-09-07: 헤더 팔레트를 걷고 **상주 줄**이 됐다
 *
 * 그전에는 헤더 아이콘(`m테마메뉴`)이 여는 **일시 pane** 이었다 — 만지는 동안에만 탭이 서고,
 * 도크를 닫으면 탭까지 걷었다. 그러다 보니 같은 것을 가리키는 손잡이가 **패널 안팎에 둘**이었고,
 * 패널을 열어보기 전에는 안에 무엇이 있는지 알 길이 없었다.
 *
 * 지금은 도크 목록의 한 줄로 늘 서 있다(`lDock` 의 목록 주석). 그래서 여기 남은 것은
 * **그 줄로 데려다주는 `open()`** 뿐이다 — 전체 찾기(⌘K)의 「테마 열기」와 소개 화면의 버튼이 부른다.
 * 「열려 있는 작업이 곧 탭」이라는 도크의 어휘는 메일 같은 작업 pane 이 진다. 테마는 작업이 아니라
 * 설정이고, **설정은 늘 같은 자리에 있어야 찾는다** — 그게 상주로 바꾼 이유다.
 *
 * ## 2026-09-15: ⌘⇧K 를 걷었다
 *
 * 켜기 전으로 되돌리는 토글(`toggle`)이 여기 있었다 — 같은 키로 껐다 켰다 하려고. 키가 빠지면서
 * (`l/테마.vue` 끝의 주석) 부르는 곳이 없어져 같이 걷었다. 되돌리기는 도크 목록에서 줄을 옮기면 된다.
 */
export function useThemePane() {
  const dock = useDock()

  /** 테마 줄을 편다(닫혀 있으면 도크까지 연다). */
  function open() {
    dock.openPane('테마')
  }

  return { open }
}
