<!--
  **「보이는 대로 고치기」 판.** 이 폴더에서 유일하게 **에디터 자체를 새로 짓는** 자리다.

  ## 무엇을 묻는 판인가

  **md 를 모르는 사람이 본문을 고칠 수 있나.**

  한동안 이 폴더에 판이 다섯이었다 — 어휘 목록(`mdc-blocks`) · 완성본 읽기(`mdc-docs`) ·
  붙여넣기 변환(`paste`) · 개발자가 md 원문을 고치는 판(`md`), 그리고 여기. 앞의 넷은
  **md 가 화면에 남는 것을 전제로** 서 있었고, 이 판이 그 전제를 버리면서 셋이 되었다가
  넷 다 지워졌다(2026-09-06). 남은 물음이 하나라서 판도 하나다.

  ---

  ## 요구 (사람이 시킨 것 — 바꾸지 말 것)

  1. **쓰는 사람은 할머니·아줌마다.** md 문법을 모르고 배울 일도 없다.
     `**`·`##`·`::` 가 화면에 **한 글자도** 있으면 안 된다.
  2. **네이버 카페 글쓰기처럼 수정이 눈에 보인다.** 글자를 끌어 고르고 누르면 그 자리에서 바뀐다.
     「편집판」과 「미리보기」로 나누지 않는다 — **화면은 하나다.**
  3. **그런데 카페 에디터를 그냥 주면 촌스러워진다.** 색·글자크기를 제 맘대로 쓰기 때문이다.
     그래서 사람이 만질 수 있는 건 우리가 정한 어휘뿐이고, 그리는 건 **테마를 탄 우리 컴포넌트**다.
  4. **사람은 md 를 평생 안 본다.** md 는 도우미(AI) 사정이다.
     (사람이 만지는 원본은 **객체**다. 저장되는 값은 md 문자열이고 그 변환은 `m/Editor/md.vue` 가 진다 —
     사람이 md 를 안 본다는 이 줄의 뜻은 어느 쪽이든 안 바뀐다.
     왜 저장이 md 인지는 그 어댑터와 `~utils/mdc/doc` 머리말.)

  ## 목적

  **완성 화면을 그대로 만지게 하는 것.** 「md 를 고치는 화면」이 아니라
  **「고쳐지는 완성 화면」**이고, md 는 그 뒤에서 나가는 형식으로만 산다.

  ## 판정 (이 판을 열고 눈으로 하는 것)

  위 요구 1·2·3 을 화면에서 그대로 짚는다 — md 기호가 한 글자도 안 보이는가, 고른 자리가 그 자리에서
  바뀌는가, 색·글자크기가 우리 어휘 안에 갇혀 있는가. **하나라도 아니면 이 방향이 틀린 것**이고, 판은 지운다.
  (한동안 이 셋을 화면 맨 위에 글로 박아 뒀는데, 판이 편집기가 아니라 설명문으로 보여서 걷어냈다 —
  2026-09-06. 판정은 눈으로 하는 것이라 화면에 글이 없어도 그대로 선다.)

  ---

  ## 지금 상태 (2026-09-06)

  **만져진다.** 아래는 `mEditor`(TipTap/ProseMirror) 이고, 샘플 md 는 `md에서문서` 로 **한 번** 객체가 되어
  들어간다. 그 뒤로는 객체가 원본이다. 맨 아래 접힌 「나가는 md」는 사람이 보는 자리가 아니라 개발자가
  `문서에서md` 가 도는지 확인하는 자리다 — 미리보기가 아니다(그리지 않는다).

  이 편집기는 읽기 화면(`<MDC>`)의 그림과 **픽셀로 같아야 한다.** 대조 상대는 실물이다 —
  신청받기 상세(`/official/enrolls/[id]`)를 옆에 열어 같은 글을 본다. (한동안 `mdc-docs` 판이 그 상대였는데
  2026-09-06에 지웠다. 모래밭끼리 비교하면 둘 다 틀려도 같아 보인다.)
  달라지면 그건 다른 물건이다. 아는 차이는 셋 — 끝에 붙는 빈 문단(타이핑할 자리), 캡션 없는 사진의 빈 캡션 줄,
  접기가 접혀 있어도 커서가 들어가면 열리는 것.

  ## 자리 — 이 판은 페이지 스크롤이라 실물의 절반만 묻는다 (2026-09-13)

**실물은 둘이다.** 게시판 글쓰기(`boards/[boardType]/write`)는 여기처럼 **페이지가 통째로 스크롤**하고,
관리 상세(`목록5` → `상세5`, 자료 상세 `/resources/[id]` 의 「고치기」도 같은 `상세5`)는 **폼 칸이 제 스크롤 칸을 가진 판떼기**다
(`m/Box/유리.vue` 의 `overflow-y-auto`). 편집기는 화면을 길게 쓰려는 물건이라 뒤쪽 자리가 원래
제 성격이 아니고, 어긋나는 자리도 거기서만 보인다 — 폰에서 본문 몇 줄만 내리면 툴바가 위로 사라진다.

그래서 위 손잡이의 `자리` 로 **같은 편집기를 두 자리에 놓아 본다.** 판떼기 쪽은 실물의 사슬을 그대로
흉내낸다 — `mBox유리`(스크롤 칸) 안에 `모델필드s2` 의 칸 여백을 두른 것이고, 그 칸의 **가로 넘침**
한 칸까지 손잡이로 뺐다. 그게 붙박이가 도는지를 가르는 유일한 자리라서다(아래).

**판정 셋이다.**

1. **판떼기에서 내리면** 툴바·띠가 칸 맨 위에 남는가.
2. **`가로넘침` 을 `hidden` 으로 돌리면 그 자리에서 안 붙는다** — `overflow-x: hidden` 이 세로를
   `auto` 로 끌어올려 그 칸을 스크롤 칸으로 만들고, 붙박이는 진짜 스크롤 칸이 아니라 늘어나기만 하는
   그 칸에 붙기 때문이다. `clip` 은 자르기만 하고 스크롤 칸이 안 된다.
3. **페이지 자리에서도 붙는다** — 화면 맨 위다. 한동안 여기만 헤더 높이만큼 내려 붙였는데 폰에서
   **흰 띠가 허공에 동동 떴다.** 이 앱의 헤더가 실제로는 안 붙박여서(`.l-header-wrap` 이 죽인다)
   없는 것을 비켜 준 셈이었다. 0 이면 크롬이 화면 맨 위에 서서 툴바로 읽힌다 — `m/Editor/Editor.vue` 의
   `top-0` 주석이 정본.

## 아직 안 되는 것

  사진·영상·목록의 **주소 넣는 손잡이**(툴바에서 막혀 있다). `m/Editor/Editor.vue` 머리말 「아직 없는 것」에 있다.

  **표의 줄·칸은 된다**(2026-09-07) — 손이 둘이다. 표에 **마우스를 얹으면** 오른쪽·아래 가장자리에 `+` 가
  떠서 맨 끝에 칸·줄을 붙이고(옵시디언·노션에서 아는 그것), 표 안에 **커서를 두면** `툴바mini` 가 표 위에 떠서
  앞에 넣기·빼기까지 준다. 칸 정렬(GFM 의 `|:---:|`)은 그때 `정렬` 묶음이 대신한다.
  여기서 짚을 것은 **위 줄을 안 편 채로** 되는가다 — 기본이 접힘이라 안 되면 손잡이가 없는 것과 같다.

  `타입지정` 연결은 **됐다**(2026-09-06) — 신청받기 상세의 「본문」 칸이 이 편집기를 쓴다.
  값의 모양이 달라서(폼은 md 문자열, 편집기는 객체) 사이에 `m/Editor/md.vue` 가 선다.

  **표 위에서 한 바퀴 도는 것도 됐다**(2026-09-06) — `/v2/docs`(`model문서`)는 본문 한 칸이 곧 행이라
  추가·저장·삭제가 편집기만 놓고 돈다. 여기(저장 없음)와 신청받기 상세(본문이 콘텐츠물에 딸린 칸)
  둘 다 못 보여주던 자리다.
-->
<template>
  <div class="mx-auto w-full max-w-[900px] px-4 py-8">
    <!-- 손잡이가 넷이라 폰 폭에서는 줄이 접힌다. 안 접으면 글자가 한 자씩 세로로 선다. -->
    <div class="mb-2 flex flex-wrap items-center justify-end gap-2">
      <!--
        **읽기·수정은 손잡이 하나로 갈아 낀다.** 이 손잡이가 답하는 물음은 하나다:
        **읽기와 수정이 눈으로 갈리는가.** 여기서 보는 것은 그림이 아니라 **갈림**이고, 그건 두 판을
        나란히 놓고 대는 것보다 **같은 자리에서 갈아 끼울 때** 더 잘 보인다. 누르면 액자(재질 축의
        면·선 + `.m-층-카드`)와 툴바가 통째로 사라지고 글만 남는다. 2026-09-07 전에는 두 모드가
        화면에서 **한 픽셀도 안 갈렸다** — 왜 그랬는지는 `m/Editor/_/틀.ts` 머리말.

        **글자가 지금 어느 모드인지를 말한다 — 눌러서 갈 곳이 아니다.** 처음엔 「읽기 모드」 한 이름을
        켜짐으로 눌러 뒀는데, 켜짐이 「지금 읽기」인지 「누르면 읽기」인지 안 갈렸다. 이름이 둘로
        갈리면(`읽기중`·`수정중`) 그 물음 자체가 없어진다. `켜짐` 은 그래서 뺐다 — 상태는 글자가 진다.

        (2026-09-07 하루는 편집판 **아래에** 같은 문서를 `readonly` 로 한 번 더 세워 뒀다. 그러니
        판이 둘로 보여서 요구 2 의 *"화면은 하나다"* 를 판 스스로 어겼다. 판은 하나고 모드가 바뀐다.)

        **그림의 대조 상대는 여전히 실물이다** — 신청받기 상세(`/official/enrolls/[id]`)를 옆에 연다. 위 머리말의
        *"모래밭끼리 비교하면 둘 다 틀려도 같아 보인다"* 가 그대로 산다.
      -->
      <mButton
        size="sm"
        역할="조용"
        :icon="읽기 ? 'i-lucide-eye' : 'i-lucide-pencil'"
        @click="읽기 = !읽기"
      >
        {{ 읽기 ? '읽기중' : '수정중' }}
      </mButton>

      <!--
        **자리** — 페이지 스크롤이냐, 스크롤 칸을 가진 판떼기냐. 머리말 「자리」가 정본이다.
        `가로넘침` 은 판떼기일 때만 뜬다 — 페이지 자리에는 그 칸 자체가 없다.
      -->
      <mButton
        size="sm"
        역할="조용"
        :icon="판떼기 ? 'i-lucide-panel-left-close' : 'i-lucide-scroll-text'"
        :켜짐="판떼기"
        title="같은 편집기를 페이지 스크롤 / 판떼기(스크롤 칸) 두 자리에 놓아 본다"
        @click="판떼기 = !판떼기"
      >
        {{ 판떼기 ? '판떼기' : '페이지' }}
      </mButton>

      <mButton
        v-if="판떼기"
        size="sm"
        역할="조용"
        :icon="가로넘침 === 'clip' ? 'i-lucide-scissors' : 'i-lucide-square-dashed'"
        :켜짐="가로넘침 === 'hidden'"
        title="칸의 가로 넘침 — hidden 이면 그 칸이 스크롤 칸이 되어 붙박이가 죽는다"
        @click="가로넘침 = 가로넘침 === 'clip' ? 'hidden' : 'clip'"
      >
        {{ 가로넘침 }}
      </mButton>

      <!-- 선택기는 제 폭을 안 지켜 감싸서 폭을 준다. 안 그러면 옆 글이 한 글자씩 세로로 선다. -->
      <div class="w-48">
        <input선택기
          v-model="고른샘플"
          type="select"
          :items="샘플s"
        />
      </div>
    </div>

    <!--
      **상자를 안 두른다.** 여기 한동안 `rounded-lg border bg-default p-6` 이 서 있었고 주석은
      「읽기 화면(신청받기 상세)이 본문을 감싸는 것과 같은 상자」라고 적혀 있었는데, **실물은 아무것도
      안 두른다** — `enrolls/[id]` 가 *"카드를 안 두른다 — 산문에 테두리를 치면 아래 문항 상자와
      같은 무게가 되어 '읽는 것'과 '쓰는 것'이 안 갈린다"* 고 못 박아 뒀다. 실물에 없는 것을 판에만
      세워 둔 셈이었고, 편집기가 제 액자를 갖게 된 지금은 겹까지 된다.
    -->
    <!--
      **껍데기만 갈아 끼우고 편집기는 하나다.** `:is` 로 바깥만 바꾸므로 두 자리에 같은 것이 선다 —
      두 벌을 적으면 한쪽만 고쳐지는 자리가 생긴다(이 폴더가 판 넷을 지운 이유와 같은 줄).
      판떼기 쪽 두 겹은 실물에서 그대로 베낀다: `mBox유리`(스크롤 칸) → 가로 넘침 한 칸
      (`view/모델필드s2.vue` 의 루트) → 칸 여백(`pl-1 pt-1 pb-2`, 같은 파일의 한 줄 아닌 칸).
    -->
    <!--
      **높이는 바깥 div 가 진다.** `mBox유리` 의 루트는 `h-full` 이라 제 높이를 안 정하고 부모를 채운다 —
      실물도 같은 꼴이다(`resources.vue` 가 `h-[calc(100dvh-헤더)]` 를 두르고 그 안이 `h-full` 로 찬다).
      유리에 직접 높이를 얹으면 `h-full` 과 같은 칸을 다투다가 **안 굴러가는 칸**이 되어, 증상이 재현 안 된다.
    -->
    <div :class="판떼기 ? 'h-[60svh]' : undefined">
      <component :is="판떼기 ? mBox유리 : 'div'">
        <div :class="판떼기 ? [가로넘침 === 'clip' ? 'overflow-x-clip' : 'overflow-x-hidden', 'pl-1 pt-1 pb-2'] : undefined">
          <ClientOnly>
            <mEditor
              v-model="문서"
              :readonly="읽기"
            />
            <template #fallback>
              <div class="text-sm text-muted">
                불러오는 중…
              </div>
            </template>
          </ClientOnly>
        </div>
      </component>
    </div>

    <!--
      **나가는 md — 개발자 확인용.** 사람은 이 자리를 안 본다(요구 4). 미리보기도 아니다 — 그리지 않고
      `문서에서md` 의 출력을 글자로 보여 줄 뿐이다. 열 때만 만든다.
    -->
    <details
      class="mt-4 rounded-lg border border-default"
      @toggle="md보기 = ($event.target as HTMLDetailsElement).open"
    >
      <summary class="cursor-pointer px-4 py-2 text-sm text-muted">
        나가는 md
      </summary>
      <pre class="max-h-[480px] overflow-auto border-t border-default px-4 py-3 text-xs leading-5 whitespace-pre-wrap text-default">{{ 나가는md }}</pre>
    </details>
  </div>
</template>

<script setup lang="ts">
import mBox유리 from '~base-comps/m/Box/유리.vue'
import { 본문샘플s } from '~utils/mdc/samples'
import { md에서문서, 문서에서md, type iPM노드 } from '~utils/mdc/doc'

definePageMeta({
  제목: '보이는 대로 고치기'
})

const 고른샘플 = ref(본문샘플s[0]!.key)
const 샘플s = computed(() => 본문샘플s.map(s => ({ label: s.제목, value: s.key })))

/** 원본. md 가 아니라 객체다. */
const 문서 = ref<iPM노드 | null>(null)

/** 읽기 모드인가. `mEditor` 의 `readonly` 는 props 라 켜고 끄면 그 자리에서 갈린다. */
const 읽기 = ref(false)

/**
 * 자리 — 판떼기(스크롤 칸) 안이냐. 머리말 「자리」가 정본이다.
 * `mBox유리` 는 `:is` 에 넘기려고 **이름으로** 들여온다(자동 임포트는 템플릿 태그에만 선다).
 */
const 판떼기 = ref(false)

/** 판떼기 안 칸의 가로 넘침. 붙박이가 도는지를 가르는 한 칸이라 손잡이로 뺐다. */
const 가로넘침 = ref<'clip' | 'hidden'>('clip')

// 들어오는 길 — 샘플(md)을 객체로. 한 번만 지난다.
watch(고른샘플, async (key) => {
  if (!import.meta.client) return
  const md = 본문샘플s.find(s => s.key === key)?.md ?? ''
  문서.value = await md에서문서(md)
}, { immediate: true })

// 나가는 길 — 열어 둔 동안만 만든다.
const md보기 = ref(false)
const 나가는md = ref('')
watch([문서, md보기], async ([d, 열림]) => {
  if (!열림 || !d) return
  나가는md.value = await 문서에서md(d)
})
</script>
