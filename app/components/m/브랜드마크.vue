<!--
  소셜 로그인 브랜드 마크 — 구글 G · 네이버 N · 카카오 말풍선 · 토스 심볼(공식 PNG).

  **아이콘 폰트(`i-simple-icons-*`)를 안 쓰는 이유.** 세 벌의 그림 크기가 제각각이다 — `kakao` 는
  말풍선이 아니라 **워드마크**라 같은 `size-5` 에서 글씨 한 줄로 쪼그라들고, `naver` 는 꽉 찬 N 이라
  옆에 서면 혼자 굵다. 한 줄에 나란히 서는 마크는 **눈에 보이는 무게**가 같아야 해서, 여기서 세 벌을
  직접 들고 브랜드마다 안쪽 크기를 따로 맞춘다(아래 `안쪽`).

  - `타일` 없음 — 브랜드 색 버튼 **안에** 얹는 맨몸 마크. N·말풍선은 `currentColor` 라 버튼 글자색을 따른다.
  - `타일` — 테마를 따르는 버튼(회색·다크) 옆에 얹는 작은 색 타일. 바탕이 무엇이든 브랜드가 읽힌다.

  색은 각 사 가이드 값이라 테마 축 밖이다.
-->
<template>
  <span
    class="inline-flex shrink-0 items-center justify-center"
    :class="[
      타일 ? (원 ? 'rounded-full' : 'rounded-[28%]') : '',
      타일 ? 타일칠[이름] : ''
    ]"
  >
    <svg
      v-if="이름 === '구글'"
      viewBox="0 0 48 48"
      :style="크기"
      aria-hidden="true"
    >
      <path
        fill="#EA4335"
        d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"
      />
      <path
        fill="#4285F4"
        d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"
      />
      <path
        fill="#FBBC05"
        d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z"
      />
      <path
        fill="#34A853"
        d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"
      />
    </svg>
    <svg
      v-else-if="이름 === '네이버'"
      viewBox="0 0 20 20"
      :style="크기"
      aria-hidden="true"
    >
      <path
        fill="currentColor"
        d="M13.56 10.7 6.25 0H0v20h6.44V9.3L13.75 20H20V0h-6.44z"
      />
    </svg>
    <!--
      토스 — **공식 심볼(2026 CI) 그림.** 음영 들어간 입체 심볼이라 원본 .ai 안에서도 벡터가 아니라 이미지다 —
      그래서 SVG 가 아니고 PNG 를 쓴다(brand.toss.im 의 Toss.zip 에서 심볼만 잘라 128px).
      타일은 흰 바탕에 파란 심볼(가이드대로 색을 안 바꾼다), 맨몸은 브랜드 파랑 버튼 안이라 흰 단색 심볼.
      흰 단색은 음영이 빠져 형태가 뭉개지니 **타일에 쓰지 않는다**(파랑 타일+흰 심볼은 말풍선 덩어리로 읽혔다).
      PNG 에 사방 여백이 15%쯤 있어 `안쪽` 값이 다른 마크보다 크다.
    -->
    <img
      v-else-if="이름 === '토스'"
      :src="타일 ? '/img/brand/toss-symbol.png' : '/img/brand/toss-symbol-white.png'"
      :style="크기"
      alt=""
      draggable="false"
    >
    <svg
      v-else
      viewBox="0 0 24 22"
      :style="크기"
      aria-hidden="true"
    >
      <path
        fill="currentColor"
        d="M12 0C5.37 0 0 4.25 0 9.5c0 3.4 2.26 6.38 5.64 8.05l-1.15 4.23c-.1.37.32.66.64.45l5.04-3.33c.6.07 1.2.1 1.83.1 6.63 0 12-4.25 12-9.5S18.63 0 12 0z"
      />
    </svg>
  </span>
</template>

<script setup lang="ts">
import type { i브랜드 } from '~/composables/use로그인'

const props = defineProps<{
  이름: i브랜드
  /** 테마 버튼 옆에 얹는 색 타일로 그린다 */
  타일?: boolean
  /** 타일을 원으로 — 알약 버튼 안에 얹을 때 */
  원?: boolean
}>()

/**
 * 틀(뿌리 `span`) 대비 그림 폭. **눈대중으로 맞춘 값이다** — 면적이 아니라 무게로 맞춘다:
 * G 는 속이 빈 고리라 크게, N 은 꽉 찬 덩어리라 작게, 말풍선은 그 사이.
 */
const 안쪽: Record<i브랜드, { 맨몸: number, 타일: number }> = {
  구글: { 맨몸: 0.9, 타일: 0.7 },
  네이버: { 맨몸: 0.64, 타일: 0.44 },
  카카오: { 맨몸: 0.84, 타일: 0.56 },
  토스: { 맨몸: 0.8, 타일: 0.9 }
}

const 크기 = computed(() => {
  /** 원은 같은 틀에서 네모보다 면이 좁다(≈78%) — 그만큼 안쪽을 줄여야 꽉 차 보이지 않는다. */
  const 비율 = 안쪽[props.이름][props.타일 ? '타일' : '맨몸'] * (props.타일 && props.원 ? 0.86 : 1)
  return { width: `${비율 * 100}%`, height: 'auto' }
})

const 타일칠: Record<i브랜드, string> = {
  /** 흰 타일(구글·토스)은 테두리를 두르지 않는다 — 브랜드 마크에 임의의 선을 더하지 않는다. */
  구글: 'bg-white',
  네이버: 'bg-[#03C75A] text-white',
  카카오: 'bg-[#FEE500] text-[#191919]',
  토스: 'bg-white'
}
</script>
