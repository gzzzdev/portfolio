/**
 * 디자인틀 화면이 아직 준비중인가. **되살릴 때 고치는 곳은 이 한 줄이다.**
 *
 * 끄면 화면 둘이 같이 돌아온다 — 캔버스(`/admin/edit/design-templates`)와 관리 목록
 * (`/admin/design-templates`). 둘을 따로 세우지 않은 이유: 관리 목록만 열어 두면 '만들기'·'복제'로
 * **좌표를 못 고치는 행**을 만들 수 있다. 저장소의 `새 디자인틀`(1002)이 그 흔적이다.
 *
 * **발급은 이 플래그와 무관하다.** 등급증·명함·회원증 버튼은 표에서 '사용중' 디자인틀을 뽑을 뿐
 * (`model회원.발급디자인틀`), 편집기를 거치지 않는다. 시드(`seeds/model디자인틀.seed.ts`)에
 * 세 용도의 기본 배치가 이미 들어 있어서 이 화면들이 막혀도 종이는 그대로 나온다.
 *
 * 코드를 지우는 대신 플래그로 두는 이유는 `용도정의s[].show` 와 같다 — 지웠다가 다시 쓰면
 * 그 사이에 갈라진 편집기(`PrintTemplate/`, 4천 줄)를 되살릴 길이 없다.
 *
 * 타입을 `boolean` 으로 못박은 것은 의도다. `true` 리터럴로 좁혀지면 편집기를 세우는 가지가
 * 통째로 죽은 코드로 읽혀서, 되살리기 전에 타입 오류부터 만난다.
 */
export const is디자인틀준비중: boolean = true
