/**
 * 조직이 가진 값 — 인쇄물에 찍히지만 **회원의 것이 아닌** 글자들.
 *
 * 폐기된 붙박이 렌더러 여기저기에 흩어져 있던 것을 한자리에 모았다. 그 층은 통째로
 * 걷어냈지만 이 값들은 렌더러의 것이 아니라 **조직의 것**이라
 * 같이 사라지면 안 된다 — 디자인틀이 `기관명`·`서명`·`증명문구` 자리를 채울 유일한 출처가 여기다.
 *
 * **아직 모델이 아니다.** 조직 설정 화면도 표도 없어서 상수로 둔다. 나중에 `model조직` 이
 * 생기면 이 파일이 그 모델의 기본값 자리로 내려가면 되고, 그때 고칠 곳은 여기 한 군데다 —
 * 값을 쓰는 쪽(`model회원.디자인틀주입`)이 이미 이 이름들만 보고 있기 때문이다.
 */
export const 조직 = {
  소속: '팀마장터',
  /** 필기체 서명 칸에 쓴다. 그 서체가 라틴 전용이라 한글을 넣으면 폰트가 죽는다 */
  영문소속: 'Majangteo',
  슬로건: '당신의 생각이 맞습니다.',
  전화번호: '+82 10 1234 5678',
  이메일: 'majangteo@gmail.com',
  웹사이트: 'https://majangteo.com',
  주소: '대한민국'
}

/**
 * 등급증 본문. 한 문장이 아니라 **한 단락**이라 줄바꿈까지가 값이다 —
 * 디자인틀의 글자 상자가 `pre-wrap` 이므로 여기 적힌 줄이 그대로 종이의 줄이 된다.
 */
export const 증명문구 = '위 사람은 마장터교육센터에서 주관하는 교육 과정을 성실히 이수하였으며,\n정해진 평가 기준을 통과하여 해당 등급을 취득하였음을 증명합니다.'
