/**
 * 출력물 — **디자인틀**(좌표를 데이터가 든다) 한 갈래뿐이다.
 *
 * 한때 여기 **AI가 색·서체만 만들고 좌표는 코드가 들던 층**이 나란히 있었다. 그러면
 * "이름을 조금 왼쪽으로"가 코드 수정이라 사람이 손댈 수 없어서 폐기했다. 걷어내면서 그 층이
 * 들고 있던 것 중 **그 층의 것이 아닌 것**만 꺼내 남겼다 — 서체표(`서체.ts`)와 조직 값
 * (`조직.ts`)이다. 둘 다 색·서체를 갈아 끼우던 쪽의 것이 아니라 각각 화면과 조직의 것이다.
 *
 * 회원증만 그리는 붙박이 렌더러도 한동안 여기 있었다. 디자인틀에 색을 칠할 자리가 없어 카드 윗단의
 * 남색 판을 그릴 방법이 없었기 때문인데, 표식이 색판(`i표식.채움`)을 받으면서 그 이유가
 * 없어졌다. 지금 회원증은 표의 `회원증_1` 행이고, 고치는 자리는 캔버스다.
 */
export * from './서체'
export * from './조직'
export * from './model애셋'
export * from './model디자인틀'
export * from './디자인틀'
