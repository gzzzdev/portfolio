/**
 * `(system)` — **도메인 데이터가 아니라 앱 자체가 서는 데 쓰는 표들.**
 * v2(과일·동물·문서…)는 화면에 그려질 데이터고, 여기 앉는 것은 화면을 그리는 규칙이거나
 * 표 자신에 대한 것이다. 그래서 v2 배럴(`~models/v2`)에서 갈라 나왔다 — 도메인 모델을 여기 두지 말 것.
 *
 * ## 이 배럴은 화면·컴포저블 전용이다
 *
 * **모델끼리는 파일을 직접 부른다**(`~models/(system)/model테이블`). 배럴로 받으면 `model테마` 가
 * 딸려오고, 그건 `useThemeStore` 를 끌고 온다 — `model메뉴`·`model역할` 이 그 한 줄 때문에
 * 테마 스토어를 그래프에 지고 있게 된다. 지금 두 자리가 파일 직접 import 인 건 그래서고,
 * 배럴로 「정리」하면 조용히 되돌아간다.
 */
export * from './model테마';
export * from './model테이블';
export * from './model표개수';
